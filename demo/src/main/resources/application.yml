logging:
  level:
    com:
      _5icodes:
        starter:
          webmvc:
            log:
              LogFilter: trace
saturn:
  enabled: true
starter:
  gray:
    app-group: GRAY
  rocketmq:
    grayTopics:
      - TEST_TOPIC
      - TEST_TOPIC_2
    consumers:
      testListener:
        grayEnable: true
feign:
  client:
    config:
      baidu:
        localMapping: http://www.5icodes.com